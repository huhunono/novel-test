[pytest]
testpaths = tests
python_files = test_*.py

markers =
    smoke: smoke tests
    contract: contract tests
    reg_ci: CI-friendly regression subset (fast, stable)
    regression: full regression tests


